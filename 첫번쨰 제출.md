4.15 작성

1. 두 실수를 메모리에 저장하고 더한 후 값을 출력하는 C 프로그램을 작성하시오.
```c
#include <stdio.h>
int main(void) {
  float a=4.2, b=4.3;
  printf("%f", a+b);
}
```
2. 두 실수를 키보드에서 읽고 메모리에 저장한 후 더한 후 화면에 출력하는 C프로그램을 작성하시오.
```c
#include <stdio.h>
int main(void) {
  float a, b;
  scanf_s("%f %f", &a, &b);
  printf("%f", a+b);
}
```
3. 두 실수를 배열에 초기화해서 저장하고 더한 후 화면에 출력 하는 C프로그램을 작성하시오.
```c
#include <stdio.h>
int main(void) {
  float a[2]={4.2, 4.3};
  printf("%f", a[0]+a[1]);
}
```
4. 두 실수를 메모리에 저장하고 함수 Add()에서 더한 후 리턴한 값을 화면체 출력하는 C언어 프로그램을 작성하시오.
```c
#include <stdio.h>

float Add(float num1, float num2) {
  return num1+num2;
}
int main(void) {
  float a=4.2, b=4.3, c;
  c= Add(a, b);
  printf("%f", c);
}
```

이제 배열을 배우게 되어 다룰 수 있는 자료의 범위가 넓어져 기쁘다.
난 github를 그저 다른 사람이 만든 프로젝트를 이용하는 것 밖에 할 수 있는게 없었는데 직접 무언가 적어 올리니 기분이 싱숭생숭하다. 한것도 없는데 이거 하나 올린다고 성장한거 같은 느낌이다
