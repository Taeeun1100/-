프로그래밍 심화 첫번째 제출

1. 모래시계 만들기

```c

#include <stdio.h>
int main(void) {
    int n;
    printf("put a number : ");
    scanf_s("%d", &n);
    for (int i = 0; i <= n / 2; i++) {
        for (int j = 0; j < i; j++) {
            printf(" ");
        }
        for (int j = 0; j < n - 2 * i; j++) {
            printf("*");
        }
        printf("\n");
    }
    for (int i = n / 2 - 1; i >= 0; i--) {
        for (int j = 0; j < i; j++) {
            printf(" ");
        }
        for (int j = 0; j < n - 2 * i; j++) {
            printf("*");
        }
        printf("\n");
    }
    return 0;
}
```
3. 각오
이번 강의에서 좋은 성적을 받는 것을 넘어, 2학년, 3학년, 취업후 회사 생활에서도 활용할 수 있도록 학습하겠습니다!!

4. 소감
혼자 고민해봤을 때 조건을 고민하다 헤멨었는데, 이번을 기점으로 모래시계를 만드는 코드를 확실히 익힐 수 있게 되었습니다. 이후로 간단하고 다양한 코드를 익혀보고 싶어졌습니다.
